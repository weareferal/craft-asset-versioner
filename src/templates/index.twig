{# @var craft \craft\web\twig\variables\CraftVariable #}
{#
/**
 * Asset Versioner plugin for Craft CMS 3.x
 *
 * Asset Versioner index.twig
 *
 * @author    Timmy O'Mahony
 * @copyright Copyright (c) 2020 Timmy O'Mahony
 * @link      https://weareferal.com
 * @package   AssetVersioner
 * @since     1.0.0
 */
#}

{% extends "_layouts/cp" %}
{% import "_includes/forms" as forms %}

{% do view.registerAssetBundle("weareferal\\assetversioner\\assetbundles\\assetversioner\\AssetVersionerAsset") %}
{% do view.registerAssetBundle("weareferal\\assetversioner\\assetbundles\\indexcpsection\\IndexCPSectionAsset") %}

{# Link for the ? icon at the bottom of the page #}
{% set docsUrl = "https://github.com/weareferal/asset-versioner/blob/master/README.md" %}

{# The title of this CP section #}
{% set title = "Asset Versioner" %}

{# The URL to this plugin's base CP section #}
{% set pluginCpUrl = url('asset-versioner') %}

{# Get a URL to an image in our AssetBundle #}
{% set iconUrl = view.getAssetManager().getPublishedUrl('@weareferal/assetversioner/assetbundles/indexcpsection/dist', true) ~ '/img/Index-icon.svg' %}

{# Content that should appear in the page header#}
{% set extraPageHeaderHtml %}
    <div class="buttons">
        <a href="{{ pluginCpUrl }}" class="btn submit add icon">{{ "Click Me!"|t('asset-versioner') }}</a>
    </div>
{% endset %}

{# The content of the CP Section#}
{% set content %}
    <h2>{{ "Index CP Section body goes here"|t('asset-versioner') }}</h2>
    <img src="{{ iconUrl }}" height="64" width="64" />
    <p class="textline"></p>
{% endset %}
