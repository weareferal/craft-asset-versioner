{% import "_includes/forms" as forms %}

{{ forms.selectField({
    first: true,
    instructions: "To be able to efficiently find the hashed versions of your files they need to be stored in memory/cache."|t('asset-versioner'),
    label: "Keystore Backend"|t('asset-versioner'),
    id: 'keystoreBackend',
    name: 'keystoreBackend',
    options: [
        { label: 'Default', value: 'default' },
        { label: 'Database', value: 'database' }
    ],
    value: settings.keystoreBackend,
    required: true,
    errors: settings.getErrors('keystoreBackend')
}) }}